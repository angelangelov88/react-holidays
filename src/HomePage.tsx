const HomePage = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      <h1>Welcome to the Holidays App!</h1>
      <p>Please select a country from the tabs above to view its public holidays.</p>
    </div>
  );
};

export default HomePage;
